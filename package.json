{
  "name": "smarterp-workspace",
  "version": "1.0.0",
  "description": "Smart ERP Workspace",
  "private": true,
  "scripts": {
    "dev": "node dev.js",
    "install-all": "npm install --prefix frontend && npm install --prefix backend"
  }
}
